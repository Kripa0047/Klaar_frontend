import { React } from 'react';
import { Layout } from 'antd';
import './Home.less';

const Home = () => {
    return (
        <Layout>
            <Layout.Header className="header">
                WELCOME
            </Layout.Header>
        </Layout>
    );
}

export default Home;